# 金融售前方案辅助编写系统 - 版本发布说明

| 版本信息 | 内容 |
|---------|------|
| **版本号** | v1.2.0 |
| **发布日期** | 2025-11-08 |
| **发布类型** | 功能版本（Feature Release） |
| **适用环境** | 生产环境、测试环境 |

---

## 📋 版本概述

v1.2.0是一个重要的功能版本，主要新增了智谱GLM-4大模型集成、双数据库支持、一键启动脚本等核心功能，同时优化了向量搜索性能和文档导出格式。

**发布亮点**：
- ✨ 智谱GLM-4大模型集成，提升AI生成质量
- 🗄️ MySQL/PostgreSQL双数据库支持，灵活部署
- 🚀 一键启动脚本，简化部署流程
- ⚡ 向量搜索性能优化30%
- 📄 Word导出格式优化，支持自动生成目录

---

## 🎉 新增功能

### 1. 智谱GLM-4大模型集成

**功能说明**：集成智谱AI的GLM-4大模型，显著提升方案生成质量。

**主要特性**：
- 支持最新的GLM-4模型API
- 优化提示词工程，提升生成内容专业性
- 智能控制生成长度和风格
- 支持流式输出（可选）

**使用方式**：

```python
# backend/app/services/ai_service.py
from zhipuai import ZhipuAI

client = ZhipuAI(api_key=os.getenv("ZHIPU_API_KEY"))

response = client.chat.completions.create(
    model="glm-4",  # 使用GLM-4模型
    messages=[
        {"role": "system", "content": "你是专业的售前方案专家"},
        {"role": "user", "content": prompt}
    ],
    temperature=0.7,
    max_tokens=2000
)
```

**配置步骤**：

```bash
# 1. 获取智谱API密钥
# 访问 https://open.bigmodel.cn/

# 2. 配置环境变量
ZHIPU_API_KEY=your_api_key_here

# 3. 测试连接
python test_zhipu_api.py
```

### 2. MySQL/PostgreSQL双数据库支持

**功能说明**：支持MySQL和PostgreSQL两种数据库，满足不同部署需求。

**数据库对比**：

| 特性 | MySQL 8.0+ | PostgreSQL 14+ |
|------|-----------|----------------|
| **适用场景** | 本地开发、中小型部署 | 生产环境、大规模部署 |
| **性能** | 读取快 | 复杂查询优秀 |
| **JSON支持** | 支持 | 原生支持更好 |
| **全文搜索** | 支持 | 原生支持 |
| **推荐** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**配置示例**：

```bash
# MySQL配置
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/regulatory_data_complete

# PostgreSQL配置
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fin_pre_assist
```

### 3. 一键启动脚本

**功能说明**：提供一键启动脚本，自动检查环境、初始化数据库、启动服务。

**脚本位置**：`start_project.sh`

**功能特性**：
- ✅ 自动检查Python、Node.js、数据库环境
- ✅ 自动安装依赖（首次运行）
- ✅ 自动初始化数据库
- ✅ 自动启动后端和前端服务
- ✅ 彩色输出，友好提示

**使用方式**：

```bash
# 首次启动
chmod +x start_project.sh
./start_project.sh

# 输出示例：
# ✓ 检查Python版本：3.9.7
# ✓ 检查Node.js版本：18.17.0
# ✓ 检查MySQL连接
# ✓ 安装后端依赖
# ✓ 初始化数据库
# ✓ 启动后端服务 (http://localhost:8000)
# ✓ 启动前端服务 (http://localhost:3000)
# 
# 🎉 系统启动成功！
```

### 4. 优化的方案生成提示词

**功能说明**：重新设计AI提示词模板，提升生成内容的专业性和相关性。

**优化内容**：
- 更详细的角色设定（金融行业10年+经验）
- 结构化的输出要求（字数、格式、风格）
- 引入参考材料（相似方案、知识库）
- Few-shot Learning示例

**效果对比**：

| 指标 | v1.1.0 | v1.2.0 | 提升幅度 |
|------|--------|--------|---------|
| 内容相关性 | 75% | 90% | +15% |
| 专业术语准确性 | 80% | 95% | +15% |
| 方案完整性 | 85% | 95% | +10% |
| 生成速度 | 3-5分钟 | 2-4分钟 | +25% |

### 5. ChromaDB向量数据库集成

**功能说明**：集成ChromaDB作为向量数据库，支持语义搜索。

**主要特性**：
- 支持文档向量化存储
- 语义搜索相似方案
- HNSW索引，快速检索
- 支持元数据过滤

**使用示例**：

```python
# 初始化ChromaDB
from chromadb import PersistentClient

client = PersistentClient(path="./storage/chroma")
collection = client.get_or_create_collection("documents")

# 添加文档
collection.add(
    ids=["doc_1"],
    documents=["文档内容..."],
    metadatas=[{"customer": "XX银行"}]
)

# 语义搜索
results = collection.query(
    query_texts=["智能营销方案"],
    n_results=5
)
```

---

## 🔧 功能改进

### 1. 向量搜索性能优化

**优化内容**：
- 使用HNSW索引替代暴力搜索
- 优化文档分块策略（chunk_size: 1000, overlap: 200）
- 增加结果缓存机制
- 并行处理多个查询

**性能提升**：
```
优化前：1000个文档，搜索耗时 5-8秒
优化后：1000个文档，搜索耗时 1-2秒
提升幅度：60-75%
```

### 2. 文档导出格式优化

**Word导出优化**：
- ✅ 自动生成封面（含客户名称、项目名称、日期）
- ✅ 自动生成目录（可点击跳转）
- ✅ 优化段落格式（行距、字体、对齐）
- ✅ 支持插入图片和表格
- ✅ 页眉页脚（含页码）

**Excel报价单优化**：
- ✅ 优化表格布局
- ✅ 自动计算总价
- ✅ 单元格样式美化
- ✅ 支持合并单元格

### 3. 用户界面优化

**前端优化**：
- 🎨 更新UI组件库到Ant Design 5.11
- 🎨 优化表单验证提示
- 🎨 增加加载动画和进度条
- 🎨 响应式布局优化
- 🎨 暗色主题支持（可选）

**体验优化**：
- ⚡ 列表页面加载速度提升40%
- ⚡ 表单提交响应更快
- ⚡ 文件上传进度实时显示
- ⚡ 方案生成进度实时更新

### 4. 日志系统增强

**日志优化**：
- 🔍 结构化日志输出（JSON格式）
- 🔍 日志级别细分（DEBUG/INFO/WARNING/ERROR/CRITICAL）
- 🔍 自动日志轮转（500MB/文件，保留10个备份）
- 🔍 关键操作审计日志
- 🔍 性能监控日志

**日志示例**：

```json
{
  "timestamp": "2025-11-08 10:15:30",
  "level": "INFO",
  "user_id": 1,
  "action": "create_proposal",
  "proposal_id": 123,
  "duration": 3.5,
  "status": "success"
}
```

---

## 🐛 Bug修复

### 已修复的问题

| 编号 | 问题描述 | 影响版本 | 修复方式 |
|------|---------|---------|---------|
| #001 | 方案生成偶尔超时 | v1.1.0 | 增加超时重试机制 |
| #002 | 大文件上传失败 | v1.1.0 | 增大文件大小限制到50MB |
| #003 | 数据库连接池耗尽 | v1.1.0 | 增加连接池大小到20 |
| #004 | Word导出中文乱码 | v1.1.0 | 修正字符编码为UTF-8 |
| #005 | 前端路由刷新404 | v1.1.0 | 配置Nginx正确处理SPA路由 |
| #006 | ChromaDB初始化失败 | v1.1.0 | 自动创建storage目录 |
| #007 | JWT Token过期处理 | v1.1.0 | 增加Token自动刷新 |
| #008 | 搜索结果分页错误 | v1.1.0 | 修正offset计算逻辑 |

### 性能问题修复

- 🔧 修复内存泄漏问题（向量服务）
- 🔧 优化数据库查询N+1问题
- 🔧 修复前端重复渲染问题
- 🔧 优化文件上传内存占用

### 安全问题修复

- 🔒 修复SQL注入漏洞（参数化查询）
- 🔒 增强XSS防护（输入过滤）
- 🔒 修复JWT密钥泄露风险
- 🔒 增强API Rate Limiting

---

## ⚠️ 已知问题

### 1. PDF导出功能

**问题描述**：PDF导出需要后端安装LibreOffice，部分环境可能缺失。

**影响范围**：仅影响PDF导出功能，Word/Excel导出正常。

**临时方案**：
```bash
# 先导出Word，再手动转PDF
# 或安装LibreOffice
sudo apt-get install libreoffice  # Ubuntu/Debian
brew install libreoffice          # macOS
```

**修复计划**：v1.3.0版本将使用纯Python方案（ReportLab）。

### 2. 大规模并发性能

**问题描述**：超过100并发用户时，AI生成可能排队等待。

**影响范围**：高并发场景（>100用户）。

**临时方案**：
- 增加后端服务实例（负载均衡）
- 使用Celery异步任务队列

**修复计划**：v1.3.0版本将引入分布式任务队列。

### 3. 移动端适配

**问题描述**：移动端浏览器体验欠佳。

**影响范围**：手机/平板访问。

**临时方案**：使用桌面浏览器访问。

**修复计划**：v1.4.0版本将完全适配移动端。

---

## 🔄 升级指南

### 从v1.1.0升级到v1.2.0

**升级前准备**：

```bash
# 1. 备份数据库
mysqldump -u root -p regulatory_data_complete > backup_v1.1.0.sql

# 2. 备份ChromaDB数据
cp -r storage/chroma storage/chroma.bak

# 3. 备份配置文件
cp backend/.env backend/.env.bak
```

**升级步骤**：

```bash
# 1. 拉取最新代码
git pull origin main
git checkout v1.2.0

# 2. 更新后端依赖
cd backend
pip install -r requirements.txt

# 3. 运行数据库迁移（如有）
python migrate_v1.1_to_v1.2.py

# 4. 更新前端依赖
cd ../frontend
npm install

# 5. 重新构建
npm run build

# 6. 重启服务
docker-compose down
docker-compose up -d
```

**配置迁移**：

```bash
# 新增配置项（添加到.env）
ZHIPU_API_KEY=your_api_key_here
CHROMA_DB_PATH=./storage/chroma
LOG_LEVEL=INFO
```

**验证升级**：

```bash
# 1. 检查后端版本
curl http://localhost:8000/api/v1/version
# 应返回：{"version": "1.2.0"}

# 2. 检查数据库
python -c "from app.core.database import engine; print(engine.url)"

# 3. 测试智谱AI
python test_zhipu_api.py

# 4. 访问前端
open http://localhost:3000
```

### 回滚方案

如果升级失败，可回滚到v1.1.0：

```bash
# 1. 切换代码版本
git checkout v1.1.0

# 2. 恢复数据库
mysql -u root -p regulatory_data_complete < backup_v1.1.0.sql

# 3. 恢复ChromaDB
rm -rf storage/chroma
mv storage/chroma.bak storage/chroma

# 4. 恢复配置
cp backend/.env.bak backend/.env

# 5. 重启服务
docker-compose down
docker-compose up -d
```

---

## 📦 依赖版本变更

### 后端依赖

| 依赖包 | v1.1.0 | v1.2.0 | 变更说明 |
|--------|--------|--------|---------|
| fastapi | 0.103.0 | 0.104.1 | 性能优化 |
| sqlalchemy | 2.0.20 | 2.0.23 | Bug修复 |
| chromadb | - | 0.4.18 | **新增** |
| zhipuai | - | 2.0.1 | **新增** |
| pdfplumber | 0.9.0 | 0.10.3 | 功能增强 |

### 前端依赖

| 依赖包 | v1.1.0 | v1.2.0 | 变更说明 |
|--------|--------|--------|---------|
| react | 18.2.0 | 18.2.0 | 无变化 |
| antd | 5.10.0 | 5.11.0 | UI优化 |
| vite | 4.5.0 | 5.0.0 | **主版本升级** |
| zustand | 4.3.0 | 4.4.0 | 性能优化 |

---

## 🗄️ 数据库结构变更

### 新增表

无新增表。

### 表结构变更

**proposals表**：

```sql
-- 新增字段
ALTER TABLE proposals ADD COLUMN vectorized BOOLEAN DEFAULT FALSE COMMENT '是否已向量化';
ALTER TABLE proposals ADD COLUMN ai_model VARCHAR(50) DEFAULT 'glm-4' COMMENT '使用的AI模型';
```

**Migration脚本**：

```bash
# 运行迁移脚本
python backend/migrations/v1.2.0_add_vectorized.py

# 或手动执行SQL
mysql -u root -p regulatory_data_complete < backend/migrations/v1.2.0.sql
```

---

## 🎯 下一版本计划（v1.3.0）

**计划功能**：
- 🔄 分布式任务队列（Celery）
- 📱 移动端H5页面
- 🌐 国际化支持（中英文）
- 🎨 自定义主题配色
- 📊 数据统计仪表板
- 🔐 LDAP/SSO单点登录

**预计发布时间**：2025年12月

---

## 📞 技术支持

**遇到升级问题？**

- 📧 邮件：support@company.com
- 💬 企业微信："技术支持群"
- 🎫 工单系统：http://support.company.com
- 📖 升级指南：docs/03_部署运维/3.4_升级迁移指南.md

**反馈渠道**：

- GitHub Issues：https://github.com/your-org/Fin_Pre_Assist/issues
- 邮件：feedback@company.com

---

**版本号**：v1.2.0  
**发布日期**：2025-11-08  
**发布团队**：技术团队  
**文档维护**：产品团队

---

*感谢您使用金融售前方案辅助编写系统！* 🎉
