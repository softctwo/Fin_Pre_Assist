# 金融售前方案辅助编写系统 - 开发者指南

| 文档信息 | 内容 |
|---------|------|
| **文档版本** | v1.2.0 |
| **发布日期** | 2025-11-08 |
| **目标读者** | 开发工程师 |
| **文档状态** | 正式发布 |

---

## 🚀 快速开始

### 环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| **Python** | 3.9+ | 后端开发语言 |
| **Node.js** | 18+ | 前端开发环境 |
| **MySQL** | 8.0+ | 数据库（本地开发） |
| **PostgreSQL** | 14+ | 数据库（生产环境） |
| **Git** | 2.0+ | 版本控制 |
| **Docker** | 20.10+ | 容器化部署（可选） |

### 克隆项目

```bash
git clone https://github.com/your-org/Fin_Pre_Assist.git
cd Fin_Pre_Assist
```

---

## 💻 后端开发环境搭建

### 1. 创建Python虚拟环境

```bash
# 进入后端目录
cd backend

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
# macOS/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# 升级pip
pip install --upgrade pip
```

### 2. 安装依赖

```bash
# 安装所有依赖
pip install -r requirements.txt

# 主要依赖包括：
# - fastapi==0.104.1        # Web框架
# - uvicorn==0.24.0         # ASGI服务器
# - sqlalchemy==2.0.23      # ORM
# - pymysql==1.1.0          # MySQL驱动
# - psycopg2-binary==2.9.9  # PostgreSQL驱动
# - chromadb==0.4.18        # 向量数据库
# - zhipuai==2.0.1          # 智谱AI SDK
# - python-docx==1.1.0      # Word处理
# - pdfplumber==0.10.3      # PDF处理
```

### 3. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑.env文件
nano .env
```

**关键配置**：

```bash
# 数据库配置
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/regulatory_data_complete

# 智谱AI配置
ZHIPU_API_KEY=your_zhipu_api_key_here

# JWT密钥
SECRET_KEY=your-secret-key-here

# ChromaDB路径
CHROMA_DB_PATH=./storage/chroma

# 日志级别
LOG_LEVEL=INFO
```

### 4. 初始化数据库

```bash
# 创建数据库
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS regulatory_data_complete CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 运行初始化脚本
python init_db.py

# 输出应显示：
# ✓ Creating table: users
# ✓ Creating table: documents
# ✓ Creating table: proposals
# ✓ Creating table: templates
# ✓ Creating table: knowledge_base
# Database initialized successfully!
```

### 5. 启动后端服务

```bash
# 开发模式（热重载）
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 或使用启动脚本
python run.py

# 访问API文档
open http://localhost:8000/api/v1/docs
```

---

## 🎨 前端开发环境搭建

### 1. 安装Node.js依赖

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 主要依赖包括：
# - react: ^18.2.0           # React框架
# - typescript: ^5.0.0       # TypeScript
# - vite: ^5.0.0            # 构建工具
# - antd: ^5.11.0           # UI组件库
# - zustand: ^4.4.0         # 状态管理
# - axios: ^1.6.0           # HTTP客户端
```

### 2. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑.env文件
nano .env
```

**配置内容**：

```bash
# API地址
VITE_API_BASE_URL=http://localhost:8000/api/v1

# 应用标题
VITE_APP_TITLE=金融售前方案辅助编写系统
```

### 3. 启动前端服务

```bash
# 开发模式
npm run dev

# 访问前端
open http://localhost:3000
```

---

## 📂 项目代码结构

### 后端目录结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI应用入口
│   ├── api/                    # API路由
│   │   ├── __init__.py
│   │   ├── auth.py            # 认证接口
│   │   ├── documents.py       # 文档管理接口
│   │   ├── proposals.py       # 方案管理接口
│   │   ├── templates.py       # 模板管理接口
│   │   ├── knowledge.py       # 知识库接口
│   │   └── search.py          # 搜索接口
│   ├── models/                 # 数据模型
│   │   ├── __init__.py
│   │   ├── user.py            # 用户模型
│   │   ├── document.py        # 文档模型
│   │   ├── proposal.py        # 方案模型
│   │   ├── template.py        # 模板模型
│   │   └── knowledge_base.py  # 知识库模型
│   ├── schemas/                # Pydantic模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── document.py
│   │   ├── proposal.py
│   │   └── common.py
│   ├── services/               # 业务逻辑
│   │   ├── __init__.py
│   │   ├── ai_service.py      # AI服务
│   │   ├── vector_service.py  # 向量服务
│   │   ├── document_processor.py  # 文档处理
│   │   ├── proposal_generator.py  # 方案生成
│   │   └── export_service.py  # 导出服务
│   ├── core/                   # 核心模块
│   │   ├── __init__.py
│   │   ├── config.py          # 配置管理
│   │   ├── database.py        # 数据库连接
│   │   ├── security.py        # 安全模块
│   │   └── exceptions.py      # 异常处理
│   └── utils/                  # 工具函数
│       ├── __init__.py
│       └── helpers.py
├── tests/                      # 测试代码
│   ├── __init__.py
│   ├── test_api.py
│   ├── test_services.py
│   └── test_models.py
├── logs/                       # 日志文件
├── storage/                    # 存储目录
│   ├── chroma/                # ChromaDB数据
│   ├── documents/             # 上传文档
│   └── exports/               # 导出文件
├── requirements.txt           # Python依赖
├── .env.example              # 环境变量模板
├── init_db.py                # 数据库初始化
├── run.py                    # 启动脚本
└── test_zhipu_api.py         # API测试脚本
```

### 前端目录结构

```
frontend/
├── public/                    # 静态资源
├── src/
│   ├── api/                  # API调用
│   │   ├── auth.ts
│   │   ├── documents.ts
│   │   ├── proposals.ts
│   │   └── index.ts
│   ├── components/           # 组件
│   │   ├── common/          # 通用组件
│   │   ├── layouts/         # 布局组件
│   │   └── features/        # 功能组件
│   ├── pages/               # 页面
│   │   ├── Login/
│   │   ├── Dashboard/
│   │   ├── Documents/
│   │   ├── Proposals/
│   │   └── Knowledge/
│   ├── stores/              # 状态管理
│   │   ├── authStore.ts
│   │   ├── documentStore.ts
│   │   └── proposalStore.ts
│   ├── types/               # TypeScript类型
│   │   ├── api.ts
│   │   └── models.ts
│   ├── utils/               # 工具函数
│   │   ├── request.ts
│   │   └── helpers.ts
│   ├── App.tsx              # 应用根组件
│   ├── main.tsx             # 应用入口
│   └── config.ts            # 配置文件
├── package.json             # npm依赖
├── tsconfig.json            # TypeScript配置
├── vite.config.ts           # Vite配置
└── .env.example            # 环境变量模板
```

---

## 📝 开发规范

### Python代码规范（PEP 8）

**命名规范**：

```python
# 模块名：全小写，下划线分隔
# 文件名: document_processor.py

# 类名：驼峰命名
class ProposalGenerator:
    pass

# 函数名：全小写，下划线分隔
def generate_proposal(request):
    pass

# 常量：全大写，下划线分隔
MAX_FILE_SIZE = 50 * 1024 * 1024  # 50MB

# 变量名：全小写，下划线分隔
proposal_id = 123
```

**代码格式**：

```python
# 导入顺序：标准库 → 第三方库 → 本地模块
import os
import sys

from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from app.core.database import get_db
from app.models.proposal import Proposal

# 每行最多79个字符
# 函数定义后空2行
# 类定义后空2行

class ProposalService:
    """方案服务类
    
    提供方案生成、编辑、导出等功能。
    """
    
    def __init__(self, db: Session):
        """初始化
        
        Args:
            db: 数据库会话
        """
        self.db = db
    
    def create_proposal(self, data: dict) -> Proposal:
        """创建方案
        
        Args:
            data: 方案数据
            
        Returns:
            创建的方案对象
            
        Raises:
            ValueError: 数据验证失败
        """
        # 实现逻辑
        pass
```

**类型注解**：

```python
from typing import List, Dict, Optional

def search_proposals(
    user_id: int,
    keyword: Optional[str] = None,
    limit: int = 20
) -> List[Dict[str, any]]:
    """搜索方案"""
    pass
```

### TypeScript代码规范

**命名规范**：

```typescript
// 接口名：驼峰命名，I开头
interface IProposal {
  id: number;
  customerName: string;
  projectName: string;
}

// 类型别名：驼峰命名
type ProposalStatus = 'draft' | 'generating' | 'completed';

// 函数名：驼峰命名（首字母小写）
function generateProposal(data: IProposalRequest): Promise<IProposal> {
  // ...
}

// 常量：全大写，下划线分隔
const MAX_FILE_SIZE = 50 * 1024 * 1024;

// 变量：驼峰命名（首字母小写）
const proposalId = 123;
```

**React组件规范**：

```typescript
import React, { useState, useEffect } from 'react';
import { Button, Form } from 'antd';

interface ProposalFormProps {
  initialData?: IProposal;
  onSubmit: (data: IProposalRequest) => void;
}

export const ProposalForm: React.FC<ProposalFormProps> = ({
  initialData,
  onSubmit
}) => {
  const [loading, setLoading] = useState(false);
  
  useEffect(() => {
    // 组件挂载逻辑
  }, []);
  
  const handleSubmit = async (values: any) => {
    setLoading(true);
    try {
      await onSubmit(values);
    } finally {
      setLoading(false);
    }
  };
  
  return (
    <Form onFinish={handleSubmit}>
      {/* 表单内容 */}
    </Form>
  );
};
```

---

## 🔧 开发工作流

### 1. 创建新功能分支

```bash
# 从main分支创建功能分支
git checkout main
git pull origin main
git checkout -b feature/add-export-pdf

# 分支命名规范：
# feature/功能名称    # 新功能
# bugfix/问题描述    # Bug修复
# hotfix/紧急修复    # 紧急修复
```

### 2. 开发新API接口

**后端步骤**：

```python
# 1. 定义数据模型 (models/example.py)
from sqlalchemy import Column, Integer, String
from app.core.database import Base

class Example(Base):
    __tablename__ = "examples"
    
    id = Column(Integer, primary_key=True)
    name = Column(String(200))

# 2. 定义Schema (schemas/example.py)
from pydantic import BaseModel

class ExampleCreate(BaseModel):
    name: str

class ExampleResponse(BaseModel):
    id: int
    name: str
    
    class Config:
        from_attributes = True

# 3. 创建API路由 (api/examples.py)
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.core.database import get_db

router = APIRouter(prefix="/api/v1/examples", tags=["examples"])

@router.post("/", response_model=ExampleResponse)
def create_example(
    data: ExampleCreate,
    db: Session = Depends(get_db)
):
    """创建示例"""
    example = Example(**data.dict())
    db.add(example)
    db.commit()
    db.refresh(example)
    return example

# 4. 注册路由 (main.py)
from app.api import examples
app.include_router(examples.router)
```

**前端步骤**：

```typescript
// 1. 定义API接口 (api/examples.ts)
import request from '@/utils/request';

export interface IExample {
  id: number;
  name: string;
}

export interface IExampleRequest {
  name: string;
}

export const createExample = (data: IExampleRequest) => {
  return request.post<IExample>('/examples', data);
};

// 2. 创建页面组件 (pages/Examples/index.tsx)
import React from 'react';
import { Button, Form, Input } from 'antd';
import { createExample } from '@/api/examples';

export const ExamplesPage: React.FC = () => {
  const [form] = Form.useForm();
  
  const handleSubmit = async (values: any) => {
    await createExample(values);
  };
  
  return (
    <Form form={form} onFinish={handleSubmit}>
      <Form.Item name="name" label="名称">
        <Input />
      </Form.Item>
      <Button type="primary" htmlType="submit">
        提交
      </Button>
    </Form>
  );
};
```

### 3. 编写单元测试

```python
# tests/test_examples.py
import pytest
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)

def test_create_example():
    """测试创建示例"""
    response = client.post("/api/v1/examples", json={
        "name": "Test Example"
    })
    assert response.status_code == 200
    data = response.json()
    assert data["name"] == "Test Example"
    assert "id" in data
```

### 4. 运行测试

```bash
# 后端测试
cd backend
pytest tests/ -v

# 前端测试
cd frontend
npm run test
```

### 5. 提交代码

```bash
# 查看修改
git status
git diff

# 添加文件
git add .

# 提交（遵循Conventional Commits规范）
git commit -m "feat: 添加示例管理功能"

# 提交消息格式：
# feat: 新功能
# fix: Bug修复
# docs: 文档更新
# style: 代码格式调整
# refactor: 代码重构
# test: 测试代码
# chore: 构建/工具变动
```

### 6. 推送并创建Pull Request

```bash
# 推送到远程
git push origin feature/add-export-pdf

# 在GitHub/GitLab创建Pull Request
# 填写PR描述，关联Issue
# 等待Code Review
```

---

## 🐛 调试技巧

### 后端调试

**1. 使用日志**：

```python
import logging

logger = logging.getLogger(__name__)

# DEBUG级别
logger.debug(f"查询参数: user_id={user_id}, limit={limit}")

# INFO级别
logger.info(f"用户 {user_id} 创建方案")

# ERROR级别
logger.error(f"AI生成失败: {str(e)}", exc_info=True)
```

**2. 使用pdb调试器**：

```python
import pdb

def generate_proposal(request):
    # 设置断点
    pdb.set_trace()
    
    # 代码继续执行
    result = some_function()
    return result
```

**3. 使用VS Code调试**：

```json
// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: FastAPI",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "app.main:app",
        "--reload",
        "--host", "0.0.0.0",
        "--port", "8000"
      ],
      "jinja": true
    }
  ]
}
```

### 前端调试

**1. 使用Console**：

```typescript
// 输出变量
console.log('proposal:', proposal);

// 输出表格
console.table(proposals);

// 性能测试
console.time('generate');
await generateProposal(data);
console.timeEnd('generate');
```

**2. 使用Chrome DevTools**：

- 打开开发者工具：`F12` 或 `Cmd+Option+I`
- Sources面板：设置断点调试
- Network面板：查看API请求
- React DevTools：查看组件状态

**3. 使用VS Code调试**：

```json
// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/frontend/src"
    }
  ]
}
```

---

## 🧪 测试指南

### 单元测试

```python
# backend/tests/test_proposal_service.py
import pytest
from app.services.proposal_generator import ProposalGenerator

@pytest.fixture
def proposal_generator():
    return ProposalGenerator()

def test_generate_executive_summary(proposal_generator):
    """测试生成执行摘要"""
    request = {
        "customer_name": "测试银行",
        "project_name": "测试项目",
        "requirements": "测试需求..."
    }
    
    summary = proposal_generator.generate_executive_summary(request)
    
    assert summary is not None
    assert len(summary) >= 200
    assert "测试银行" in summary
```

### 集成测试

```python
def test_proposal_creation_flow():
    """测试完整的方案创建流程"""
    # 1. 登录
    response = client.post("/api/v1/auth/login", json={
        "username": "test_user",
        "password": "test_pass"
    })
    token = response.json()["access_token"]
    
    # 2. 创建方案
    response = client.post(
        "/api/v1/proposals",
        json={
            "customer_name": "测试银行",
            "project_name": "测试项目",
            "requirements": "测试需求..."
        },
        headers={"Authorization": f"Bearer {token}"}
    )
    assert response.status_code == 200
```

---

## 📦 常用开发命令

### 后端命令

```bash
# 启动开发服务器
uvicorn app.main:app --reload

# 运行测试
pytest tests/ -v

# 代码格式化
black app/
isort app/

# 代码检查
flake8 app/
mypy app/

# 数据库迁移
alembic revision --autogenerate -m "描述"
alembic upgrade head

# 生成requirements.txt
pip freeze > requirements.txt
```

### 前端命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 运行测试
npm run test

# 代码检查
npm run lint

# 代码格式化
npm run format

# 类型检查
npm run type-check
```

---

**文档版本**：v1.2.0  
**最后更新**：2025-11-08  
**维护团队**：开发团队

---

*快乐编码！Happy Coding!* 🎉
