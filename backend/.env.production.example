# =============================================================================
# 生产环境配置模板
# =============================================================================
# ⚠️ 警告: 生产环境配置，请仔细审查所有配置项
# 使用: cp .env.production.example .env.production

ENVIRONMENT=production

# Application
APP_NAME=金融售前方案辅助系统
DEBUG=False  # ⚠️ 生产环境必须关闭DEBUG
LOG_LEVEL=INFO

# Server
HOST=0.0.0.0
PORT=8000

# Database (⚠️ 使用生产数据库)
DATABASE_URL=postgresql://prod_user:CHANGE_THIS_PASSWORD@db-host:5432/fin_pre_assist_prod
DATABASE_ECHO=False

# Redis (⚠️ 使用生产Redis)
REDIS_HOST=redis-host
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD
REDIS_DB=0

# CORS (⚠️ 仅允许实际域名)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# JWT (⚠️ 必须使用强密钥)
# 生成命令: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_TO_PRODUCTION_SECRET_KEY_AT_LEAST_32_CHARACTERS
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI (使用智谱GLM-4)
ZHIPU_API_KEY=your-production-zhipu-api-key
ZHIPU_MODEL=glm-4
AI_PROVIDER=zhipu

# Performance
ENABLE_METRICS=False  # ⚠️ 生产环境建议关闭公开metrics端点

# File Storage (⚠️ 使用持久化存储)
UPLOAD_DIR=/data/fin_pre_assist/documents
EXPORT_DIR=/data/fin_pre_assist/exports
MAX_UPLOAD_SIZE=52428800

# ChromaDB (⚠️ 使用持久化路径)
CHROMA_PERSIST_DIRECTORY=/data/fin_pre_assist/chroma

# Security (生产环境强化)
# - SECRET_KEY 至少32位
# - 密码策略: 8字符+大小写+数字
# - API限流: 登录5/分, 注册10/分, AI生成10/时
# - CORS严格限制
# - DEBUG关闭，错误信息脱敏
# - 日志压缩，保留30天
